services:
  cardwatch:
    build: .
    container_name: cardwatch
    ports:
      - "8099:8099"
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    environment:
      - CONFIG_PATH=/app/config/config.yaml
    restart: unless-stopped
