[project]
name = "oscam-monitor"
version = "0.1.0"
description = "OScam monitoring tool with channel mapping, stats, and web dashboard"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"
